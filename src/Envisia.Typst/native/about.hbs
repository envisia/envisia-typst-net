# Third-party notices

The native library in this package contains the [Typst](https://typst.app/) compiler and the Rust crates it depends
on. Their license texts follow. This file is generated by [cargo-about](https://github.com/EmbarkStudios/cargo-about)
from `src/Envisia.Typst/native/Cargo.lock`.

| License | Crates |
| --- | ---: |
{{#each overview}}
| {{{name}}} | {{count}} |
{{/each}}

{{#each licenses}}
## {{{name}}}

Used by:

{{#each used_by}}
- {{crate.name}} {{crate.version}}{{#if crate.repository}} ({{{crate.repository}}}){{/if}}
{{/each}}

```
{{{text}}}
```

{{/each}}
